<h1>Add a new cocktail</h1>

<%= simple_form_for @cocktail do |f| %>
  <%= f.input :name %>
  <%= render 'dose_form' if @cocktail.id.present? %>
  <%= f.button :submit, class: 'btn' %>
<% end %>

<%= link_to 'Back', cocktails_path %>

