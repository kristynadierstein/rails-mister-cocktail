<%= simple_form_for[ @cocktail, @dose ] do |f| %>
  <%= f.label :name %>
  <%= f.input :name %>

  <%= f.label :ingredients %>
  <%= f.select :ingredient.name  %>

  <%= f.label :doses %>
  <%= f.select :doses %>

  <%= f.submit %>
<% end %>
